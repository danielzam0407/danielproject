# local tooling
.claude/

# OS
Thumbs.db
desktop.ini
.DS_Store

# dependencias y build del worker
node_modules/
agente/.build/

# estado del emulador local de wrangler (KV y D1 de mentiras) y secretos de
# desarrollo. Nada de esto es del proyecto: es de esta computadora.
.wrangler/
.dev.vars

# El motor de prospección. Los datos y los sitios generados NO van al repo:
# es público en GitHub, y la lista de a quién le estamos escribiendo no lo es.
datos/

# `p/` SI va al repo: son las propuestas publicadas, y el push es el deploy.
